package com.hernan.empresaapp.model.enums;

/**
 * Roles del sistema. Definen qué puede hacer cada usuario autenticado.
 * ADMIN: acceso total. EMPLEADO: operaciones del día a día (ventas, inventario).
 */
public enum Rol {
    ADMIN,
    EMPLEADO
}
