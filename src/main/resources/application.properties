spring.application.name=empresaapp

# --- Base de datos MySQL ---
spring.datasource.url=jdbc:mysql://localhost:3306/empresa_app
spring.datasource.username=root
spring.datasource.password=HernanDB5276#

# Hibernate crea/actualiza tablas automáticamente según las entidades JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- JWT (cambiar el secret en producción) ---
app.jwt.secret=MiClaveSecretaSuperSeguraParaEmpresaApp2026CambiarEnProduccion
app.jwt.expiration-ms=86400000

# Usuario administrador inicial (solo se crea si no existe ningún usuario)
app.admin.username=admin
app.admin.password=admin123
app.admin.email=admin@empresa.com

# Carga datos de demostración al iniciar (solo si no existen aún)
app.demo.seed=false

# CORS local (en producción: application-prod.properties)
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
